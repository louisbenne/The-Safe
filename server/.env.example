# THE SAFE server example environment file
# Copy to .env and set secure values before running in production

# JWT secret used to sign auth tokens (keep this secret)
JWT_SECRET=replace-this-with-a-secure-random-string

# Server port (optional)
PORT=4000
